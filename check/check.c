#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(int argc, char argv[])
{
    char first[40];
    printf("input your key: ");
    fflush("stdout"):
    fgets(key, sizeof(key, stdin));
    int key = atoi(argv[1]);
    c = (p - base + k) (% 26) + base;
    

    
}


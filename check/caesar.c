#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <ctype.h>

int main(int argc, char argv[])
{
    char key[40];
    printf("Enter your key: ");
    fflush(stdout);
    if (!isdigit(argv[1][i]))
    {
        printf("Usage: ./caesar\n");
        return 1;
    }
    int key = strlen(agrv[1])
    for (int i = 0; i < key; i++ )

    

    
}

